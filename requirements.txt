# Единственная настоящая зависимость сборщика: без неё не читаются PDF,
# а в PDF лежат правила Венгрии и KAIST.
pypdf>=5.0

# Полный набор корневых сертификатов. Без него Python не видит сайты
# европейских университетов, подписанные GEANT: браузер подтягивает
# корни по требованию, Python — нет.
certifi>=2024.0
