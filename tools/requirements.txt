# Зависимости сборщика данных. Сайту они не нужны: он читает готовый
# data/index.json и не имеет ни одной зависимости вообще.
#
# Ставить: python -m pip install -r tools/requirements.txt
#
# pypdf — чтение PDF. Половина государственных стипендий публикует
# настоящие требования не на странице, а в PDF: Stipendium Hungaricum,
# MEXT и другие. Без него они недоступны конвейеру целиком.
pypdf>=6.0
