// Адрес прокси с ИИ-объяснением (api/explain.js, развёрнутый на Vercel).
//
// Пока стоит null, кнопки «Объяснить» на сайте нет вовсе, и он работает
// как раньше — без единого запроса наружу. Когда функция развёрнута,
// впиши адрес: '/api/explain', если сайт стоит там же, или полный
// 'https://имя-проекта.vercel.app/api/explain', если в другом месте
// (тогда в Vercel нужен ALLOWED_ORIGINS с адресом сайта).
export const EXPLAIN_URL = null;
